import { ServiceTransactionItem } from './service-transaction-item';

describe('ServiceTransactionItem', () => {
  it('should create an instance', () => {
    expect(new ServiceTransactionItem()).toBeTruthy();
  });
});
