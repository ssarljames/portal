import { PrintTransactionItem } from './print-transaction-item';

describe('PrintTransactionItem', () => {
  it('should create an instance', () => {
    expect(new PrintTransactionItem()).toBeTruthy();
  });
});
