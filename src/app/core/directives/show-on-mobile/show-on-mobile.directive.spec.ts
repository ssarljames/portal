import { ShowOnMobileDirective } from './show-on-mobile.directive';

describe('ShowOnMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnMobileDirective();
    expect(directive).toBeTruthy();
  });
});
